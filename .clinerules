# UNIX時間表示するCLIツール

# 技術

- TypeScript

# 仕様

- 実行されると秒単位のUNIX時間を出力する。
- コマンドライン引数 ns を指定されるとnano秒単位のUNIX時間を出力する。

# コーディングガイドライン

- オブジェクト指向による設計
  - データと振る舞いを class にカプセル化
  - tell, don't ask
- 単一責任原則に従ったモジュール分割
  - ここでいうモジュールは JavaScript のモジュール(=ファイル)
